# Графический интерфейс
PyQt6>=6.7,<7

# Офлайн-распознавание речи.
# ВНИМАНИЕ: версия зафиксирована намеренно. Последняя версия на PyPI (0.3.45)
# не собрана под macOS вообще — там нет ни одного macOS-колеса, и установка
# на Apple Silicon падает. 0.3.44 — последняя версия с macosx_10_6_universal2.
vosk==0.3.44

# Захват звука с микрофона (несёт собственный PortAudio, brew-пакет не нужен)
sounddevice>=0.5,<0.6

# Обработка звука перед распознаванием: фильтрация и подавление пауз.
# На чистом Python цикл по блоку держал бы GIL и мешал аудиопотоку.
numpy>=1.26

# Нечёткое сравнение слов
rapidfuzz>=3.9,<4

# Получение текстов с аккордами из сети
requests>=2.32,<3
beautifulsoup4>=4.12,<5
lxml>=5.2

# Опционально, только macOS: системное распознавание речи (точнее vosk,
# но работает лишь когда приложение собрано в .app через build_app.py)
pyobjc-framework-Speech>=10.0 ; sys_platform == "darwin"
pyobjc-framework-AVFoundation>=10.0 ; sys_platform == "darwin"
